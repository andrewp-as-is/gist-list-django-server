0) 100500 {% if cond1 or cond2 or cond3 %}
1) xxx_selected = True
в конкретной view?
минус в том что даже когда наследовано то будет тоже самое
НО... в custom случае даже когда наследовано там уже custom {% if request.path %}


2) или еще как вариант передавать view/urls name/module, но с другой стороны это какая-то мутная инфа из другого слоя. если из другого слоя и брать то уже подготовленное должно быть для этого слоя (templates)
3) можно вообще все items генерировать :)
---
context_data.key как вариант тк явный naming что сделано в get_context_data
+   проще debug
+   xxx_list сам по себе
-   чуть сложнее реализация
---
не избыточно ли Карл?
единственное что... но не единственное тк debug
почему context_data? а не context, не data, не просто {{ var }}
---
context_data btn
как передавать btn всякие опции
context_data.xxx.selected
context_data.xxx.disabled

1) context_data.gists_selected
2) context_data.btn.gists_selected
3) context_data.btn.gists.selected

в любом случае тогда придется везде прописывать gists_selected = False в других местах. дублирование кода
nav_selected = 'gists'
nav_selected = 'starred'
selected = 'starred'
но selected может быть 100500 вариантов
xxx_selected
selected['xxx'] = 'todo'
context_data.details.language.xxx
context_data.details_language.xxx


Select order
